services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: coolify-proxy-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
    restart: unless-stopped
